import React from 'react'
import './sideNav.css'

import NavContent from '../NavContent'

const SideNav = (props) => {
    return(
        <div id="side-nav">
            <NavContent/>   
        </div>
    )
}

export default SideNav